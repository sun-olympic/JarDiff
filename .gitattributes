# 默认：文本自动规范化，检出为 LF
* text=auto eol=lf

# Windows 脚本必须 CRLF，否则 CMD 解析报错
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# Unix shell 必须 LF
*.sh    text eol=lf

# 二进制文件不做换行处理
*.png   binary
*.ico   binary
*.icns  binary
*.ttf   binary
*.jar   binary
*.dmg   binary
*.exe   binary
